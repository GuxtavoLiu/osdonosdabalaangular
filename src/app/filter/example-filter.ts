import {GeneralFilter} from "./general-filter";

/**
 * @filtro é utilizado para enviar os filtros para o backend.
 *
 * @classname ExampleFilter
 * @author NextAge
 */
export class ExampleFilter extends GeneralFilter {
    id?: number;
    email?: number;
}
